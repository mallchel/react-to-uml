export enum TestEnum {
  one = 'one',
  two = 'two',
}

export enum TestEnum2 {
  one = 'one',
  two = 'two',
}

export enum TestEnum3 {
  one = 'one',
  two = 'two',
}

export type PredefinedToolId = TestEnum | TestEnum2 | TestEnum3;
