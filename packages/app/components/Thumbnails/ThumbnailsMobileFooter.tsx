import { UiComponent } from '@ui/kit';
import { ReactNode } from 'react';

export const ThumbnailsMobileFooter = ({ children }: { children: ReactNode }) => {
  return (
    <>
      ThumbnailsMobileFooter
      {children}
      <UiComponent />
    </>
  );
};
